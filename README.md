# Nazare

Opinionated Shopify Liquid theme and component registry built for speed.

## Install

Nazare CLI requires Node.js.

Install latest CLI:

```sh
curl -fsSL https://raw.githubusercontent.com/fedorivanenko/nazare/main/install.sh | sh
```

Verify install:

```sh
nazare --help
```
