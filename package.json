{
	"name": "nazare",
	"version": "0.11.4",
	"description": "Opinionated Shopify Liquid theme and component registry built for speed.",
	"bin": {
		"nazare": "./bin/nazare.js"
	},
	"scripts": {
		"test": "npm run test:unit",
		"test:build": "vitest run test/integration/theme-build.test.js",
		"test:build:slow": "NAZARE_RUN_SLOW_TESTS=1 vitest run test/integration/theme-build.test.js",
		"test:unit": "vitest run test/*.test.js"
	},
	"license": "UNLICENSED",
	"private": true,
	"devDependencies": {
		"vitest": "^4.1.7"
	}
}
