{
	"name": "nazare-theme",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "set -a; [ -f .env ] && . ./.env; set +a; shopify theme dev",
		"build": "vite build",
		"watch": "vite build --watch"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.0.0",
		"tailwindcss": "^4.0.0",
		"vite": "^7.0.0"
	}
}
